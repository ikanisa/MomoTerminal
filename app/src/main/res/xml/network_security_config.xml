<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for MomoTerminal
    
    IMPORTANT: Before deploying to production, replace the placeholder certificate
    pins with actual SHA-256 pins from your SSL certificate chain:
    1. Get your server's certificate: openssl s_client -connect lhbowpbcpwoiparwnwgt.supabase.co:443
    2. Extract the public key and generate the pin:
       openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | openssl enc -base64
    3. Replace the pins below
-->
<network-security-config>
    <!-- Disable cleartext traffic globally -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Supabase domain - Certificate pinning disabled for debug builds -->
    <!-- Enable pinning in production by uncommenting the pin-set below -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">lhbowpbcpwoiparwnwgt.supabase.co</domain>
        <!-- 
        <pin-set expiration="2026-01-01">
            <pin digest="SHA-256">YOUR_PRIMARY_PIN_HERE</pin>
            <pin digest="SHA-256">YOUR_BACKUP_PIN_HERE</pin>
        </pin-set>
        -->
    </domain-config>
    
    <!-- Debug configuration for development -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
